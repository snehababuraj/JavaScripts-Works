//string methods

  //charAt(index):return character at specified index
  //indexOf(character):return index position of particular character , otherwise it will return -1
  //toUpperCase:convert to uppercase
  //toLowerCase:convert to lowercase
  //startsWith("str"):return true if tha character seq is start with given letter
  //endsWith(srt):return true if tha character seq is ends with given letter
  //includes("str"):return true if tha character is present in the text
  //slice(start,end):
  //split()
  //there is no method ot sort a string in js

  var text="sasi funda"
//   console.log(text.charAt(2));
    //  console.log(text.indexOf("f"));
    // console.log(text.toUpperCase());
    // console.log(text.toLowerCase());
    // console.log(text.startsWith("t"));
    // console.log(text.endsWith("a"));
    // console.log(text.includes("a"));
    // console.log(text.slice(8,13));
    console.log(text.split(" "));

